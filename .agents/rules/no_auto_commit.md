---
description: Prevents automatic git commits
---

# No Auto-Commit Rule

Do NOT automatically run `git commit` or `git push` after making code changes.
Always wait for the USER to explicitly say "commit" or "push" before performing these actions.
